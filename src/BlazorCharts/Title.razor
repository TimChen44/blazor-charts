@inherits Element<BcTitle>

@if (Config != null && IsInited==true)
{
    <g class="bc-title">
        <text x="@TitleX" y="@TitleY"   text-anchor="@TextAnchor.ToDescription()" font-size="@FontSize">@Config?.Title</text>
    </g>
}